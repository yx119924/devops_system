import { createStore } from 'vuex'
import user from './modules/user'

export default createStore({
  modules: {
    user
  },
  // 全局状态
  state: {
  },
  // 全局getters
  getters: {
  },
  // 全局mutations
  mutations: {
  },
  // 全局actions
  actions: {
  }
})